% !TEX root = ../main.tex

\section{Method}

\subsection{Problem Setting}
% One paragraph: bounded-harness history-conditioned formulation. Define task stream x_1...x_T with x_t ~ P_t, history H_t, harness C_t = phi(H_t) with |C_t| <= K, action a_t ~ pi(a | x_t, C_t).

\subsection{Theoretical Framework}

\subsubsection{Harness and Regret}
% One paragraph: define utility V(C, x) and regret of the harness relative to full-history utility.

\subsubsection{Regret Decomposition}
% One paragraph: state E[Regret] = L_evo(Phi, K) + L_nav(phi) and interpret each term (evolver-class limitation vs. conditioning limitation).

\subsubsection{When Each Loss Matters}
% One paragraph: L_nav = 0 under stationarity; L_nav >= Sigma_t / 4 under shift - maps to C1. This is why static benchmarks do not surface the issue.

\subsubsection{Reducing Each Loss}
% One paragraph: expanding Phi reduces L_evo (Theorem 4); conditioning the harness reduces L_nav; the two are orthogonal (Theorem 5), motivating the two contributions as composable.

% FIGURE 2: Regime table placeholder.
\begin{figure}[t]
\centering
\rule{0.45\textwidth}{3cm}
\caption{Regime table: which contribution matters under which (stationarity $\times$ complexity) combination. \textbf{Placeholder.}}
\label{fig:regime_table}
\end{figure}

\subsection{Contribution 1: Multi-Agent Evolution}
% One paragraph: 4-phase structured evolution (Analyst -> Researchers -> Builder -> Verifier) with persistent cross-cycle state (task_board.md, research_log.jsonl, architecture.md).
% Addresses C2(a) construction gap and C3 distillation. HITL hooks at credential and task-board stages address C2(b) direction gap under severe shift.

% FIGURE 3: Multi-agent orchestration flow diagram.
\begin{figure}[t]
\centering
\rule{0.45\textwidth}{4cm}
\caption{Multi-agent evolution pipeline: Analyst $\to$ Researchers (parallel) $\to$ Builder $\to$ Verifier, with HITL integration points. \textbf{Placeholder.}}
\label{fig:multi_agent_flow}
\end{figure}

\subsection{Contribution 2: Navigation via Strategy Tree}
% One paragraph: git-backed strategy tree; F_Navigate reads each branch's workspace (README, system prompt, skills, tools) via git show; routes task to best-fit branch.
% Inline mode (single sandboxed evolver LLM call with agentic branching) and orchestrated mode (plan-driven).
% Addresses C1 directly and C3 capacity-partitioning.

% FIGURE 4: Strategy tree + per-task routing.
\begin{figure}[t]
\centering
\rule{0.45\textwidth}{4cm}
\caption{Strategy tree: main + specialized branches. F\_Navigate reads each branch's workspace via git and routes each task; solver runs on the selected branch workspace. \textbf{Placeholder.}}
\label{fig:strategy_tree}
\end{figure}

\subsection{Joint Architecture}
% One paragraph: how multi-agent and navigation compose end-to-end (navigation engine hosts the structured-evolution template).
% Each contribution handles its own failure mode alone; C3 requires both jointly - multi-agent distills across cycles, navigation partitions across branches.
