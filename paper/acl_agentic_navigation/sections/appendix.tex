% !TEX root = ../main.tex

\section{Full prompts}
\label{app:prompts}

\subsection{F\_Navigate system prompt}
% Navigator system prompt, one code block

\subsection{Analyst, Researcher, Builder, Verifier system prompts}
% One code block per role.

\subsection{HITL trigger prompts}
% Credential request template and task-board directive template.

\section{Algorithm listings}
\label{app:algos}

\subsection{Algorithm 1: Navigation-aware evolution loop}
% Pseudocode of the main batch loop with navigation.

\subsection{Algorithm 2: Multi-agent 4-phase cycle}
% Pseudocode of the structured evolution template (Analyze $\to$ Research $\to$ Build $\to$ Verify).

\section{Benchmark details}
\label{app:benchmarks}

\subsection{PolyBench}
% Data collection, temporal-order enforcement, resolution criteria,
% confidence gate, CWR computation (capital-weighted return),
% APY / Sharpe definitions.

\subsection{CTF-Dojo}
% pwncollege/ctf-archive source, challenge categorization,
% 13-year temporal ordering, Docker sandbox details,
% flag verification via hash comparison.

\subsection{FutureX}
% FutureX-Past task collection, language/source slices (zh-finance vs. en-wiki),
% strict (Wikipedia revision API) vs. live (DuckDuckGo) search modes,
% cutoff-date enforcement.

\section{Additional tables and figures}
\label{app:extra}

\subsection{Per-batch accuracy curves (all three benchmarks)}
% Figure placeholder: 3 panels, full-evo vs. early-freeze vs. navigation per benchmark.

\subsection{Per-regime slice accuracy}
% Table placeholder: PolyBench certainty bins, CTF era buckets, FutureX language buckets.

\subsection{Artifact-type distribution across ablations (all benchmarks)}
\label{app:compute_sanity}
% Extends RQ2 Table: per-benchmark artifact histograms + compute-matched single-agent control.

\subsection{Shuffled-order control}
\label{app:shuffled}
% Table placeholder: full-evo non-monotonicity disappears under temporal shuffling;
% navigation gain collapses to noise.

\subsection{Per-branch routing statistics}
% Table placeholder: number of tasks routed to each branch, per-branch pass rate.

\subsection{Harness size trajectory}
% Line plot placeholder: per-solve size over cycles, all methods, all benchmarks.

\section{Case studies}
\label{app:cases}

\subsection{A navigation success}
% One task narrative: shift detected, branch created, subsequent same-regime tasks routed correctly.

\subsection{A construction success}
% One multi-file infrastructure artifact the multi-agent evolver built that single-agent could not.

\subsection{A HITL success}
\label{app:hitl_trace}
% zh-finance credential trace from RQ4, before/after pass-rate.

\subsection{A failure case}
% One case where all three contributions failed; discussion of what would be required.

\section{Limitations}
\label{app:limitations}

\paragraph{Benchmark coverage.} Three benchmarks, three domains (prediction markets, cybersecurity, forecasting). The D1/D2/D3 frame's cross-domain generality requires testing on domains we have not yet covered (robotics, biomedical, long-form writing).

\paragraph{Evolver class is not scalar.} Our $\Levo$ argument is diagnostic, not predictive. Multi-agent's empirical advantage is demonstrated via ablation and artifact-type analysis (RQ2), not a formal class-expansion bound.

\paragraph{Human-in-the-loop cost model.} We measure cost by human time per invocation. In a production deployment, organisational cost (specialist availability, SLA) may dominate. RQ4 uses the author as oracle.

\section{Ethics statement}
\label{app:ethics}
% Short paragraph on dataset licenses, no PII, authorized CTF archive use,
% HITL protocol approval.

\section{Reproducibility statement}
\label{app:reproducibility}
% Point to code release, config files per benchmark, per-cycle logs,
% random-seed policy (solver temperature 0; evolver Bedrock retries with adaptive backoff).
