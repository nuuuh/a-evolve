\documentclass[11pt]{article}

% ACL submission skeleton.
% Replace this with the official ACL style files when drafting:
%   \usepackage[review]{acl}
% Keep section bodies in sections/*.tex. This entrance file should contain
% only the preamble, title/author block, abstract, and section includes.

\usepackage{times}
\usepackage{latexsym}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{url}

\title{Agentic Evolution under Long-Horizon, Non-Stationary, Open-Ended Task Streams}

\author{
  % TODO: Add author list, affiliations, and contribution footnotes.
}

\begin{document}
\maketitle

\begin{abstract}
% Write a 5--6 sentence abstract aligned with the three-dimensional frame.
% Sentence 1: State the setting: agentic evolution on real-world deployments
% where tasks are simultaneously long-horizon (D1), non-stationary (D2), and
% open-ended (D3). Static/i.i.d. benchmarks omit all three.
% Sentence 2: State the empirical puzzle and three challenges:
% (C1) early-freeze beats full-evo on every temporal benchmark we test;
% (C2) a single-agent prompt-level evolver cannot construct the infrastructure
% real deployment requires;
% (C3) harness size grows 18--60x across a short horizon, exceeding any bounded
% solver context.
% Sentence 3: Introduce the theoretical frame: regret decomposes into
% evolution loss L_evo and navigation loss L_nav, which are orthogonal and
% independently improvable.
% Sentence 4: State the two contributions: multi-agent evolution (4-phase
% structured pipeline with HITL) expands the evolver class and reduces L_evo
% (C2); navigation via a git-backed strategy tree conditions the harness per
% task and reduces L_nav (C1); both are jointly required for C3.
% Sentence 5: Summarize empirical setup: PolyBench (5,075 markets / 16 days),
% CTF-Dojo (261 challenges / 13 years), FutureX (332 tasks / 82 days);
% four-cell ablation grid {single-agent, multi-agent} x {no-nav, nav}, plus a
% temporally shuffled control that isolates non-stationarity as the mechanism.
% Sentence 6: Core results after final numbers lock in.
\end{abstract}

\input{sections/introduction}
\input{sections/method}
\input{sections/experiment}
\input{sections/conclusions}
% Note: sections/conclusions.tex holds the Conclusions paragraph; sections/appendix.tex
% (included below) holds Related Work + Limitations + Ethics + Reproducibility stubs that
% currently live outside the main flow. Reshuffle before submission.

% Uncomment these lines once references.bib contains real entries and the draft
% has at least one \cite{...}. The current outline intentionally has no
% citations, so enabling BibTeX now makes latexmk fail on a missing .bbl file.
% \bibliographystyle{plain}
% \bibliography{references}

\appendix
\input{sections/appendix}

\end{document}
