Your previous output was REJECTED because it did not follow the required format.

Common mistakes:
- Missing numeric count (WRONG: 'All tasks fail', RIGHT: '6 tasks fail')
- Conversational text instead of structured bullets

Rewrite using EXACTLY this structure:

## Failure Patterns (Cycle {evo_number})
- <regime>: <NUMBER> tasks fail because <reason>. PRIORITY: HIGH|MEDIUM|LOW
(EVERY bullet MUST start with a number)

## Verified Capabilities

## Unresolved

## Human Requests

Here is what you wrote:
{previous_output}
